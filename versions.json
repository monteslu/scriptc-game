{
  "scriptc": {
    "version": "0.0.17",
    "source": "fork",
    "fork": "https://github.com/monteslu/scriptc",
    "branch": "game-integration",
    "note": "integration branch: merges the topic branches below, which are kept SEPARATE and each self-contained so any one can be sent upstream as its own PR without dragging the others along",
    "topic_branches": {
      "fix/ffi-const-binding": "a manifest-bound FFI call is dropped when its result initializes a never-reassigned local (upstream issue vercel-labs/scriptc#21)",
      "fix/msvc-ssize-t": "scr_runtime.h declares ssize_t function pointers; MSVC has no ssize_t, so any Windows build through that header fails to parse",
      "fix/msvc-posix-time": "clock_gettime and nanosleep are called by five runtime TUs; mingw ships both, MSVC ships neither, so an x86_64-windows-msvc build fails to compile"
    }
  },
  "canvas": {
    "CANVAS_VERSION": "1.0.0",
    "source": "build-libcanvas release (local out/ checkout wins when present)",
    "skia_gl_standard": "gl",
    "release_tag": "v1.0.0-jsg8"
  },
  "sdl": {
    "version": "2.32.10",
    "source": "system libsdl2-dev"
  },
  "webaudio": {
    "version": "0.8.1",
    "source": "https://github.com/monteslu/webaudio-node",
    "commit": "34a1ff8738b4ea128d860158209e416ffa708def",
    "note": "C++ graph compiled native by scripts/build-webaudio.sh; source is UNPATCHED (shim/emscripten.h and shim/wasm_simd128.h stand in for the two headers it wants), so a bump is a re-fetch. Pinned by FULL sha: actions/checkout cannot fetch an abbreviated one.",
    "commit_short": "34a1ff8"
  }
}
